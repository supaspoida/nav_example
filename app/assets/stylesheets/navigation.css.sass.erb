$selectors: <%= Navigation.selectors %>

=active
  background-color: blue
  &, a
    color: red

@each $selector in $selectors
  ul.#{$selector}
    li.#{$selector}
      +active

ul li.active
  +active
